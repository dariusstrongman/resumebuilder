# Workflow backups contain live API keys (OpenAI, Stripe, Supabase service, html2pdf)
# Never commit to public repo
workflow-backups/

# Internal docs - keep local, do NOT publish
CLAUDE.md
SETUP.md

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
resume example/
schema.sql

# Test artifacts (real or synthetic resumes used for local testing)
long_test_resume.*
sample_resumes/
v2_init.sql
.claude/
